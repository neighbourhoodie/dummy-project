'use script'

const assert = require('assert')

describe('project', () => {
  it('does maths', () => {
    assert.equal(2 + 2, 4)
  })
})

describe('main', () => {
  it('is ok', () => {})

  it('builds pull requests', () => {})
})
